// This file serves as the main entry point for all integration tests.
// It declares modules for subdirectories within the `tests/` folder,
// allowing Cargo to discover and run all tests contained within them.

mod core;
mod integration;
